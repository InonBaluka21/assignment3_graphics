# assignment3_graphics

Things to install for part 2:

pip install "numpy>=1.21.0" "torch>=1.9.0" "matplotlib>=3.4.0" "plotly>=5.0.0" "tqdm>=4.62.0"
pip install ipykernel jupyter
pip install ipykernel

Activating the venv in terminal in Task2_PointNet dir:
venv\Scripts\activate

Load the kernel of the venv to run the notebook:
Press ctrl+shift+P
Type there this => Developer: Reload Window
look for the venv kernel.
if it doesn't show up, write in the terminal (where the venv appears in the beggining of the line) this: python -m ipykernel install --user --name=venv --display-name "Python (venv)"
